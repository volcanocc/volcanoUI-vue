const state = {
}

// actions
const actions = {
    // actionName({commit}, status) {
    //     commit('mutationName', status)
    // }
}

// mutations
const mutations = {
    // mutationName(state, packageObject) {
    //     state.packageInfo = packageObject
    // }
}

// getters
const getters = {
    // gettersName: state => state.gettersName
}

export default {
    state,
    getters,
    actions,
    mutations
}
